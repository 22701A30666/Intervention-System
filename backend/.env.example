# Backend configuration
PORT=4000
# Postgres connection string (Supabase or managed Postgres)
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
# n8n webhook URL to notify mentor dispatcher
N8N_WEBHOOK_URL=https://your-n8n-instance/webhook/mentor-dispatcher